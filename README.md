# 🐍 Django + ⚛️ React Boilerplate

## Usage
```
docker-compose build
docker-compose up
```
The Django server will be running on http://localhost:8000 while the React server will be running on http://localhost:3000.